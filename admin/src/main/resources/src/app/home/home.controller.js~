'use strict';

angular.module('home')

    .controller('HomePageController', function ($scope, SideMenu) {

//        $scope.menu = SideMenu.getMenu();
        SideMenu.getPermitted().then(function (menu) {
            $scope.menu = menu;
        });



    });
